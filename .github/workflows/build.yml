name: Build Project From Source Files

on:
  pull_request:
    types: [ synchronize ]


jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ ubuntu-latest, windows-latest, macOS-latest ]